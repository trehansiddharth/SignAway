imgData = char(input('Enter image data to visualize: '));

numPixels = length(imgData) / 2
dimension = sqrt(numPixels)

if mod(dimension, 2) == 0
    image = zeros(dimension, dimension);
    i = 1;
    j = 1;
    while ~isempty(imgData)
        digit1 = imgData(1);
        imgData(1) = [];
        
        d
    end
else
    sprintf('Invalid data length.')
end
